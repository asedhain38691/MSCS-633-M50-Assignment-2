## QR Code Generator

For this assignment, I create a Python application that generates a QR code from a user-provided URL.

### How to Run
1. Install dependencies:
   pip install -r requirements.txt
2. Run the application:
   python qr_code_generator.py
